"""Placeholder boundary for a future TEN native runtime adapter.

This module intentionally does not import FastAPI, aiortc, or the current
TEN-compatible gateway. Future native TEN code should call the conversation
core through contracts instead of depending on transport/runtime details.
"""
